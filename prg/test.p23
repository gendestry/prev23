var i : int;
var i : int, j : char;

typ test = int;
typ x = int, y = bool;

fun neki() : bool;
fun neki(y: int) : int;
fun neki(y: int, x: char) : char;
fun neki(y: int, x: char) : int, neki2() : bool;
fun neki(y: int, x: char) : int, neki2(y:int, x:char, z:bool) : void;

fun delaj() : int = a + b;
fun delaj() : int = {
    if x == 3 then {
        a
    }
    else {
        b
    };

    while (a < b) do a;

    -a + b * (3 & c.x)
};
